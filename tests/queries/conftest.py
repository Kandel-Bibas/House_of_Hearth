"""Queries-specific test helpers.

The `queries_seed` fixture is defined in `tests/conftest.py` so it's available
to both `tests/queries/` and `tests/api/`. This file is kept as a placeholder
for future queries-specific fixtures.
"""
